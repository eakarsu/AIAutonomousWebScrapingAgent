const test=require('node:test');const assert=require('node:assert/strict');const{isPublicAddress,normalizeUrl,validateJob,validateResolvedAddresses,canonicalRecord}=require('../domain/crawlPolicy');const h='a'.repeat(64);const valid={allowedHosts:['example.com'],startUrls:['https://example.com/items#x'],requestsPerMinute:10,robotsPolicy:'enforce',robotsEvidenceDigest:h,extractionSchema:{type:'object',properties:{title:{type:'string'}}},maxPages:10};
test('blocks private networks and off-list hosts',()=>{assert.equal(isPublicAddress('10.0.0.1'),false);assert.throws(()=>normalizeUrl('https://evil.example/x',['example.com']),/allowlist/);assert.throws(()=>validateResolvedAddresses(['192.168.1.2']),/non-public/);});
test('creates deterministic policy plan',()=>assert.equal(validateJob(valid).plan.startUrls[0],'https://example.com/items'));
test('canonical record enforces contract',()=>assert.equal(canonicalRecord({canonicalUrl:'https://example.com/a',data:{title:'A'},sourceObservedAt:'2026-01-01T00:00:00Z',contentDigest:h},validateJob(valid).plan).data.title,'A'));
test('rejects uncontracted fields',()=>assert.throws(()=>canonicalRecord({canonicalUrl:'https://example.com/a',data:{secret:'x'},contentDigest:h},validateJob(valid).plan),/outside contract/));
